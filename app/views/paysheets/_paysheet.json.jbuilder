json.extract! paysheet, :id, :dias_laborados, :neto_pagado, :id, :comisiones, :auxilio_trasporte, :recargos, :prima, :prestamos, :salud, :pension, :created_at, :updated_at
json.url paysheet_url(paysheet, format: :json)
