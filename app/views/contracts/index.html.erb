<p id="notice"><%= notice %></p>
<div class="container">
  <div class="card mx-auto mt-5">
    <div class="card-header"><h1>Contratos</h1></div>
    <div class="card-body">
<div class="form-group">
  <div class="form-row">
    <div class="col-md-12">
<table class="table table-bordered">
  <thead>
    <tr>
      <th>Id</th>
      <th>Salario</th>
      <th>Cargo</th>
      <th>Id empresa</th>
      <th>Tipo contrato</th>
      <th>Fecha ingreso</th>
      <th>Fecha retiro</th>
      <th colspan="3">Opciones</th>
    </tr>
  </thead>
  <tbody>
    <% @contracts.each do |contract| %>
      <tr>
        <td><%= contract.id %></td>
        <td><%= contract.salario %></td>
        <td><%= contract.cargo %></td>
        <td><%= contract.id_empresa %></td>
        <td><%= contract.tipo_contrato %></td>
        <td><%= contract.fecha_ingreso %></td>
        <td><%= contract.fecha_retiro %></td>
        <td><%= link_to 'Ver', contract,{ class: "btn btn-info btn-sm" } %></td>
        <td><%= link_to 'Editar', edit_contract_path(contract),{ class: "btn btn-success btn-sm" } %></td>
        <td><%= link_to 'Eliminar', contract, :class => "btn btn-danger btn-sm", method: :delete, data: { confirm: 'Está Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="form-group">
 <div class="form-row">
  <div class="col-md-12">
    <center><%= link_to 'Nuevo Contrato', new_contract_path,{ class: "btn btn-warning btn-sm" } %></center>
  </div>
 </div>
</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>